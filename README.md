# Web Socket-Slack

## Pre-installation
- nodejs
- aws
  - aws `IAM`
  - aws `kinesis`
- `config.js` - using `config.js.template`
  - `slack`
    - `RTM_CONN_URL`
  - `kinesis`
    - `region`
    - `streamName`

## Setup
```
$ npm install
$ node index.js
```
